接口安全部分：
1、反爬虫（操作太频繁，不让访问之类的）---------方法是加上头信息，refer,cookie
2、接口加密（sign）
3、一层身份（带着一层身份来请求，在请求头中带着什么ip（只能局域网访问）之类的方法）


jmeter---断言部分
0、协议状态码
1、业务状态码
2、具体的数据


jmeter--授权管理器
1、基本认证---base
2、常规认证
3、自定义认证
4、auth2.0

jmeter--cookie管理
会自动带上服务器返回的seesion_id，然后请求需要带登陆信息的接口时，会自动带上